/**
 * Javascript Portugol
 * https://github.com/moacir/jspt
 *
 * Copyright (c) 2013 Moacir de Oliveira
 * Licensed under the MIT license.
 */

(function () {

'use strict';

var Context = require('../context').context,
    slice = Array.prototype.slice,
    std = new Context();

std.setFunction('leia', function () {
	return prompt("Insira o valor da variavel:");
    //global.terminal.pause(false);
    //var test = global.terminal.set_prompt("4");
    //alert(test);
    //return global.terminal.get_prompt();
    //return global.terminal.read();
	//global.terminal.insert("4");
	//return global.terminal.get_command();
    //var c;
    //return JSON.stringify(global.terminal.push());
    global.terminal.pause(true);
});

std.setFunction('imprima', function () {
    var args = slice.call(arguments, 0);
    if (args.length > 0) {
        global.terminal.echo(args.join("").replace(/["]+/g, ''));  
    }
});

exports.module = std;

})();
